# 筛选状态下删除列撤销问题修复

## 问题描述

在筛选状态下删除列之后再撤销，数据会变成没有筛选过的数据，并且筛选列表的勾选状态不正确。

## 问题根因分析

### 1. 删除列操作的处理流程
当在筛选状态下删除列时：
1. 删除列数据
2. 更新筛选条件中的列索引（删除列后面的列索引需要减1）
3. 重新执行筛选
4. 保存历史记录（包含原始的筛选状态）

### 2. 撤销操作的原始流程
撤销删除列时：
1. 恢复配置和筛选状态（通用恢复逻辑）
2. 恢复删除的列数据
3. ❌ **问题所在**：没有重新执行筛选

### 3. 问题的核心
- 删除列时筛选条件的列索引被修改了
- 撤销时虽然恢复了原始筛选条件，但列数据结构已经改变
- 需要重新执行筛选来确保筛选结果正确

## 修复方案

### 修改文件：`src/hooks/sheet/hooks/useHistory.js`

在撤销删除列的逻辑中添加了以下处理：

```javascript
// 特殊处理：对于删除列的撤销，需要重新恢复筛选状态
// 因为删除列时筛选条件的列索引被修改了，撤销时需要恢复原始的列索引
if (state.filterState && state.filterState.filtered && state.filterState.filtered.length > 0) {
    // 重新设置筛选状态（覆盖之前在通用恢复中设置的状态）
    sheet.config.filtered = [...state.filterState.filtered]
    
    // 清空并重新设置筛选数据
    sheet.filterCellData.clear()
    if (state.filterState.filterCellData) {
        state.filterState.filterCellData.forEach((value, key) => {
            sheet.filterCellData.set(key, value)
        })
    }
    
    sheet.rowMapping = Array.isArray(state.filterState.rowMapping)
        ? [...state.filterState.rowMapping]
        : []

    // 重新执行筛选，因为列数据结构发生了变化
    await sheet.hooks.toolsHook.filterByCheckedSilent(sheet.config.filtered)
}
```

## 修复的关键点

### 1. 数据结构恢复
- 先恢复删除的列数据
- 更新列数量（`sheet.config.colCount += deleteCount`）

### 2. 筛选状态恢复
- 重新设置筛选条件（使用原始的列索引）
- 清空并重新设置筛选数据映射
- 恢复行号映射

### 3. 重新执行筛选
- 使用 `filterByCheckedSilent` 重新执行筛选
- 避免界面闪烁，提供流畅的用户体验

## 测试场景

### 场景1：基本筛选状态撤销
1. 对某列进行筛选
2. 删除另一列
3. 撤销删除操作
4. ✅ 筛选状态保持，数据显示正确

### 场景2：多列筛选状态撤销
1. 对多个列进行筛选
2. 删除非筛选列
3. 撤销删除操作
4. ✅ 多条件筛选完全保持

### 场景3：删除筛选列后撤销
1. 对某列进行筛选
2. 删除正在筛选的列
3. 撤销删除操作
4. ✅ 筛选列和筛选条件都被恢复

## 技术细节

### 执行顺序的重要性
```javascript
// 正确的执行顺序：
1. 恢复列数据结构
2. 更新列数量
3. 重新设置筛选状态
4. 重新执行筛选
```

### 筛选状态的完整恢复
```javascript
// 需要恢复的筛选相关数据：
- sheet.config.filtered      // 筛选条件
- sheet.filterCellData       // 筛选后的数据映射
- sheet.rowMapping           // 行号映射
```

### 性能优化
- 使用 `filterByCheckedSilent` 避免界面闪烁
- 异步执行筛选操作
- 清理相关缓存提高后续操作性能

## 影响范围

### 直接影响
- 修复了筛选状态下删除列撤销的问题
- 确保筛选列表勾选状态正确同步

### 间接影响
- 提高了撤销操作的可靠性
- 改善了用户体验，避免数据状态混乱

## 验证要点

1. **数据完整性**：撤销后列数据完全恢复到删除前状态
2. **筛选状态**：筛选条件和筛选结果正确恢复
3. **UI同步**：筛选列表的勾选状态与实际筛选条件一致
4. **列索引**：筛选条件中的列索引正确对应恢复后的列
5. **性能**：撤销操作流畅，无明显卡顿

## 总结

这次修复解决了一个复杂的状态同步问题，确保了在筛选状态下进行列操作时的数据一致性。通过正确的执行顺序和完整的状态恢复，用户现在可以安全地在筛选状态下进行列删除和撤销操作，而不会丢失筛选状态或出现数据显示错误。
