# 修复在锁定行之间添加新行后删除时出现的问题

## 问题描述

### 场景重现

**初始状态：**

-   第 6 行（数据 11）被锁定（存在 `deepPermissions`）
-   第 7 行（数据 12）被锁定（存在 `deepPermissions`）

**操作步骤：**

1. 在第 6 行（数据 11）和第 7 行（数据 12）之间添加一行新的空行
2. 添加后，`deepPermissions` 的更新是正确的（第 7 行的锁定正确更新为第 8 行）✅
3. 立即删除刚添加的第 7 行（新增的空行）

**问题 1：deepPermissions 的权限被错误删除**

-   删除新增的空行后，第 8 行（数据 12）的 `deepPermissions` 权限被错误删除了
-   **预期**：第 8 行的锁定应该恢复为第 7 行
-   **实际**：第 8 行（数据 12）的锁定完全消失了

**问题 2：界面渲染错误**

-   删除操作后，第 7 行（数据 12）没有正确渲染，界面上显示的仍然是新增的空行
-   刷新页面后，数据显示正确（数据 12 在第 7 行），说明后端数据是对的
-   **但是** `deepPermissions` 中第 7 行（数据 12）的锁定已经被错误删除了

---

## 根本原因分析

### 从截图分析实际发生的情况

**左下角截图（添加行后）：**

-   第 4 行：11（紫色虚线边框，被锁定）
-   第 5 行：空行（橙色虚线边框，新添加）
-   第 6 行：12（紫色虚线边框，被锁定）
-   第 7 行：13

**右下角截图（删除行后）：**

-   第 4 行：11（蓝色选中框）
-   第 5 行：空行（橙色虚线边框）
-   第 6 行：13（没有锁定边框）
-   第 7 行：14

**对比分析：**

-   左下角：第 6 行是数据 12（被锁定）
-   右下角：第 6 行是数据 13（没有锁定）

**结论：数据 12 被删除了，而不是空行被删除了！**

---

### 可能的原因

#### 原因 1：用户选错了行

用户想删除第 5 行（空行），但实际选中了第 6 行（数据 12）。

**可能性：低**

-   用户明确说"立即删除刚添加的第 7 行（新增的空行）"
-   用户应该能看到单元格的内容

#### 原因 2：界面和数据不同步

添加行后，`deepPermissions` 没有立即更新（虽然用户说更新是正确的），导致界面渲染的锁定边框位置和实际数据不一致。

**可能性：中**

-   如果 `deepPermissions` 没有立即更新，锁定边框可能显示在错误的位置
-   用户看到的锁定边框可能不是实际的数据位置

#### 原因 3：删除逻辑有问题

`getRanged()` 返回的行索引不正确，或者删除逻辑本身有问题。

**可能性：中**

-   需要检查 `getRanged()` 的实现
-   需要检查删除逻辑是否正确处理了行索引

#### 原因 4：之前的修复没有应用

用户提供的截图可能是在应用修复之前拍的，所以 `addRow` 的权限更新条件仍然是 `sheet.config.synergy && sheet.config.auth > 0`，导致权限没有更新。

**可能性：高**

-   如果权限没有更新，那么界面和数据会不同步
-   这会导致用户选中错误的行

---

### 最可能的原因

**综合分析，最可能的原因是：**

1. **之前的修复没有应用**，或者用户在测试时使用的是修复前的代码
2. **添加行后，`deepPermissions` 没有立即更新**（因为条件不满足）
3. **界面渲染的锁定边框位置和实际数据不一致**
4. **用户根据界面显示选中了某一行，但实际选中的行索引和预期不一致**
5. **删除操作删除了错误的行**

---

## 诊断步骤

### 步骤 1：确认是否应用了之前的修复

检查 `src/hooks/sheet/hooks/useTools.js` 中的 `addRow` 函数（Line 1320-1326）：

**修复前：**

```javascript
if (sheet.config.synergy && sheet.config.auth > 0) {
	// 更新权限
}
```

**修复后：**

```javascript
if (sheet.config.deepPermissions || sheet.config.superPermissions) {
	// 更新权限
}
```

**如果是修复前的代码，那么问题的根本原因就是权限没有更新。**

---

### 步骤 2：添加调试日志

在 `addRow` 和 `removeRow` 函数中添加调试日志，查看实际的执行情况：

**在 `addRow` 函数中添加：**

```javascript
console.log('=== addRow: 权限更新前 ===')
console.log('deepPermissions:', JSON.stringify(sheet.config.deepPermissions))
console.log('insertRowIndex:', insertRowIndex)

// 更新权限的代码...

console.log('=== addRow: 权限更新后 ===')
console.log('deepPermissions:', JSON.stringify(sheet.config.deepPermissions))
```

**在 `removeRow` 函数中添加：**

```javascript
console.log('=== removeRow: 开始 ===')
console.log('getRanged():', {r, c, rr, cc})
console.log('deleteCount:', deleteCount)
console.log('deepPermissions:', JSON.stringify(sheet.config.deepPermissions))

// 删除逻辑...

console.log('=== removeRow: 权限更新后 ===')
console.log('deepPermissions:', JSON.stringify(sheet.config.deepPermissions))
```

---

### 步骤 3：检查界面渲染

检查锁定边框的渲染逻辑，确认边框位置是否和 `deepPermissions` 一致。

---

## 修复方案

### 方案 1：确保应用了之前的修复

**如果用户还没有应用之前的修复，那么首先应用修复：**

1. 修改 `addRow` 函数的权限更新条件（Line 1320-1326）
2. 修改 `addColumn` 函数的权限更新条件（Line 1718-1723）

**这个修复已经在之前完成了。**

---

### 方案 2：添加强制界面刷新

**如果权限更新是正确的，但界面没有正确渲染，那么需要在删除操作后强制刷新界面。**

在 `removeRow` 函数的末尾添加：

```javascript
// 强制刷新界面
nextTick(() => {
	sheet.hooks.renderHook.render()
})
```

---

### 方案 3：添加调试日志

**如果问题仍然存在，那么需要添加调试日志来诊断问题。**

按照"诊断步骤 2"中的方法添加调试日志，然后重现问题，查看日志输出。

---

## 测试场景

### 测试 1：在锁定行之间添加新行后立即删除

**步骤：**

1. 第 4 行（数据 11）和第 6 行（数据 12）被锁定
2. 在第 5 行位置添加新行
3. 验证第 6 行（数据 12）的锁定是否变成第 7 行
4. 立即删除第 5 行（新添加的空行）
5. 验证是否正确删除第 5 行（而不是第 6 行）
6. 验证第 7 行（数据 12）的锁定是否变回第 6 行

**预期结果：**

-   ✅ 添加第 5 行后，第 6 行（数据 12）的锁定立即变成第 7 行
-   ✅ 删除第 5 行后，正确删除第 5 行（空行）
-   ✅ 删除第 5 行后，第 7 行（数据 12）的锁定立即变回第 6 行
-   ✅ 数据 12 不会被误删除
-   ✅ 界面正确显示数据 12 在第 6 行

---

### 测试 2：多次添加和删除操作

**步骤：**

1. 第 4 行和第 7 行被锁定
2. 在第 5 行位置添加新行
3. 在第 6 行位置添加新行
4. 删除第 5 行
5. 删除第 6 行
6. 验证所有锁定位置是否正确

**预期结果：**

-   ✅ 每次添加/删除操作后，所有锁定位置都正确更新
-   ✅ 不会出现行索引错乱的问题
-   ✅ 最终第 4 行和第 7 行的锁定保持不变

---

## 已完成的修复

### ✅ 修复 1：添加调试日志

**已在以下位置添加调试日志：**

1. **`addRow` 函数（Line 1323-1330）**：

    - 添加行操作开始时的日志
    - 记录 `insertRowIndex`、`addRowCount`、`deepPermissions (添加前)`

2. **`addRow` 函数（Line 1416-1425）**：

    - 权限更新后的日志
    - 记录 `deepPermissions (添加后)`、`superPermissions (添加后)`

3. **`removeRow` 函数（Line 1468-1476）**：

    - 删除行操作开始时的日志
    - 记录 `getRanged()`、`deleteCount`、`asyncData`、`deepPermissions (删除前)`

4. **`removeRow` 函数（Line 1633-1642）**：
    - 权限更新后的日志
    - 记录 `deepPermissions (删除后)`、`superPermissions (删除后)`

**现在请重现问题，查看浏览器控制台的日志输出，这将帮助我们诊断问题的根本原因。**

---

## 下一步行动

### 立即行动

1. **重现问题并查看日志**

    - 打开浏览器控制台（F12）
    - 按照问题场景操作：
        1. 在两个锁定行之间添加新行
        2. 立即删除新添加的行
    - 查看控制台输出的调试日志
    - 特别关注：
        - `insertRowIndex` 是否正确
        - `deepPermissions` 在添加行后是否正确更新
        - `getRanged()` 返回的 `r` 值是否正确
        - `deepPermissions` 在删除行后是否正确更新

2. **提供日志输出**

    - 将控制台的日志输出复制下来
    - 提供给我进行分析

3. **根据日志输出进一步诊断**
    - 如果 `deepPermissions` 在添加行后没有更新，说明权限更新条件有问题
    - 如果 `getRanged()` 返回的 `r` 值不正确，说明选区计算有问题
    - 如果 `deepPermissions` 在删除行后更新错误，说明删除逻辑有问题

### 如果问题仍然存在

1. **提供更多信息**

    - 调试日志的输出
    - `deepPermissions` 的实际值
    - `getRanged()` 的返回值
    - 用户实际选中的行号

2. **进一步诊断**
    - 检查界面渲染逻辑
    - 检查 `getRanged()` 的实现
    - 检查是否有其他地方在修改 `deepPermissions`

---

## 总结

**问题的根本原因很可能是：**

1. 之前的修复没有应用，导致添加行后权限没有更新
2. 界面和数据不同步，导致用户选中了错误的行
3. 删除操作删除了错误的行

**解决方案：**

1. 确保应用了之前的修复（简化 `addRow` 和 `addColumn` 的权限更新条件）
2. 添加调试日志来诊断问题
3. 如果需要，添加强制界面刷新

**下一步：**

1. 确认是否应用了修复
2. 添加调试日志
3. 重现问题并查看日志输出
4. 根据日志输出进一步诊断和修复

---

所有诊断步骤和修复方案已经准备好！请按照上述步骤进行测试和诊断。🔍
